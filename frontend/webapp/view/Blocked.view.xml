<mvc:View
  controllerName="hedgecontrol.controller.Blocked"
  xmlns:mvc="sap.ui.core.mvc"
  xmlns="sap.m">
  <Page
    title="{blocked>/title}"
    showNavButton="true"
    navButtonPress=".onNavBack"
    class="sapUiResponsiveContentPadding">
    <content>
      <IllustratedMessage
        title="{blocked>/title}"
        description="{blocked>/description}"
        illustrationType="sapIllus-UnableToLoad"
        illustrationSize="Auto">
        <additionalContent>
          <VBox class="sapUiSmallMarginTop">
            <Title level="H5" text="{i18n>blockedBlockersTitle}" />
            <List items="{blocked>/blockers}" noDataText="{i18n>blockedNoBlockers}">
              <StandardListItem title="{blocked>}" icon="sap-icon://warning" />
            </List>
          </VBox>
        </additionalContent>
      </IllustratedMessage>
    </content>
  </Page>
</mvc:View>
